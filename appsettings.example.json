//To create an appsettings.json file, copy this file into the project's root and rename it to "appsettings.json"
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "Your connection string goes here"
  },
  "SmtpSettings": {
    "SMTP_EMAIL": "mail@seed_project.com.mx",
    "SMTP_PASSWORD": "capibarasRule",
    "SMTP_HOST": "seed_project.com.mx",
    "SMTP_PORT": 9000
  }
}
// To get any value from appsettings.json use an instance of IConfiguration
// E.g. _config.GetSection("FirstKey").GetSection("KeyInsideFirstKey").Value;
